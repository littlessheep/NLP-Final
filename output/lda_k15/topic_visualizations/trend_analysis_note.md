# Topic Trend Analysis

No topic trend plot was generated because the current dataset only provides abstract text. There is no reliable year, publication date, or time variable in `abstracts.csv`. If a year column is added later, topic proportions from `output/lda_k15/lda_k15_doc_topic_distribution.csv` can be aggregated by year to produce trend plots.
